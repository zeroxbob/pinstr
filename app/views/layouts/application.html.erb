<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Pinstr" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Include Tailwind CSS %>

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50 font-sans text-gray-900 leading-relaxed">
    <header class="bg-gradient-to-r from-indigo-600 to-purple-600 text-white shadow-md">
      <div class="max-w-6xl mx-auto px-6 py-4 flex flex-col md:flex-row justify-between items-center gap-4">
        <h1 class="text-2xl font-semibold"><a href="/" class="text-white hover:text-gray-200 transition-colors">Pinstr</a></h1>
        <div class="flex gap-4 md:gap-6 items-center flex-wrap justify-center md:justify-end">
          <% if current_user %>
            <a href="/bookmarks" class="text-white/90 hover:text-white hover:bg-white/10 px-4 py-2 rounded transition-all">My Bookmarks</a>
            <a href="/bookmarks/new" class="text-white/90 hover:text-white hover:bg-white/10 px-4 py-2 rounded transition-all">New Bookmark</a>
            <%= button_to "Logout", logout_path, method: :delete, class: "text-white/90 hover:text-white hover:bg-white/10 px-4 py-2 rounded transition-all bg-transparent border-none cursor-pointer font-inherit text-base" %>
          <% else %>
            <a href="/sessions/new" class="text-white/90 hover:text-white hover:bg-white/10 px-4 py-2 rounded transition-all">Login</a>
          <% end %>
        </div>
      </div>
    </header>

    <div class="min-h-screen">
      <div class="max-w-6xl mx-auto px-6 py-4" data-controller="flash-message">
        <% flash.each do |key, message| %>
          <div class="<%= key == 'alert' ? 'bg-red-50 text-red-800 border border-red-200' : 'bg-green-50 text-green-800 border border-green-200' %> px-4 py-3 rounded-lg my-4 font-medium"><%= message %></div>
        <% end %>
        
        <div class="bg-white rounded-lg shadow-lg p-8 my-8">
          <%= yield %>
        </div>
      </div>
    </div>
  </body>
</html>
